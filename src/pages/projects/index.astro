---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/navigation/Header.astro';
import Footer from '../../components/navigation/Footer.astro';

// Get all projects
const allProjects = await getCollection('projects');

// Sort projects by date (newest first) or featured status
const sortedProjects = allProjects.sort((a, b) => {
  // Featured projects first
  if (a.data.featured && !b.data.featured) return -1;
  if (!a.data.featured && b.data.featured) return 1;
  
  // Then by date
  return new Date(b.data.startDate).valueOf() - new Date(a.data.startDate).valueOf();
});

// Group projects by category
const categories = [...new Set(allProjects.map(project => project.data.category))];
const projectsByCategory = categories.reduce((acc, category) => {
  acc[category] = sortedProjects.filter(project => project.data.category === category);
  return acc;
}, {} as Record<string, typeof sortedProjects>);

function formatDate(date: Date) {
  return new Intl.DateTimeFormat('en-US', {
    year: 'numeric',
    month: 'long',
  }).format(date);
}

// Define category metadata
const categoryMeta = {
  'platform-projects': {
    title: 'Platform Engineering',
    description: 'Internal platforms and developer experience tools',
    icon: '🏗️',
    color: 'from-blue-500/20 to-cyan-500/20'
  },
  'open-source': {
    title: 'Open Source',
    description: 'Community contributions and open source projects',
    icon: '🌟',
    color: 'from-green-500/20 to-emerald-500/20'
  },
  'case-studies': {
    title: 'Case Studies',
    description: 'Real-world implementations and outcomes',
    icon: '📋',
    color: 'from-purple-500/20 to-pink-500/20'
  }
};
---

<BaseLayout 
  title="Projects | Geoffrey Miller - Platform Engineering Portfolio & Case Studies"
  description="Explore platform engineering projects, open source contributions, and real-world case studies from Geoffrey Miller's 12+ years building scalable cloud infrastructure."
>
  <Header />
  
  <main class="container mx-auto px-4 py-8 max-w-7xl 3xl:max-w-8xl">
    <!-- Hero Section -->
    <section class="mb-12">
      <div class="hero bg-base-100 shadow-xl rounded-2xl">
        <div class="hero-content text-center py-12">
          <div class="max-w-full">
            <h1 class="text-4xl font-bold text-glow mb-4">Projects & Portfolio</h1>
            <p class="text-xl text-base-content opacity-70">
              A showcase of platform engineering projects, open source contributions, and real-world 
              case studies demonstrating scalable infrastructure solutions and developer experience innovations.
            </p>
          </div>
        </div>
      </div>
    </section>

    {sortedProjects.length > 0 ? (
      <>
        <!-- Featured Projects -->
        {sortedProjects.filter(p => p.data.featured).length > 0 && (
          <section class="mb-16">
            <h2 class="text-2xl font-bold text-glow mb-8 flex items-center gap-3">
              <span class="w-1 h-6 bg-accent rounded-full"></span>
              Featured Projects
            </h2>
            <div class="grid md:grid-cols-1 lg:grid-cols-2 xl:grid-cols-2 2xl:grid-cols-3 gap-6 lg:gap-8">
              {sortedProjects.filter(p => p.data.featured).slice(0, 3).map((project) => {
                const meta = categoryMeta[project.data.category as keyof typeof categoryMeta];
                return (
                  <article class={`card bg-base-100 shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-[1.02] relative overflow-hidden group`}>
                    <div class="card-body p-6 lg:p-8">
                    {/* Animated background effect */}
                    <div class="absolute -inset-x-4 -inset-y-4 z-0 bg-gradient-to-r from-accent/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 blur-xl"></div>
                    <div class="relative z-10">
                    <div class="flex items-center gap-4 mb-6">
                      <span class="text-3xl">{meta?.icon || '🚀'}</span>
                      <div>
                        <div class="badge badge-primary">
                        {meta?.title || project.data.category}
                        </div>
                        <div class="flex items-center gap-4 mt-2">
                          <time class="text-sm text-base-content opacity-70">
                            {formatDate(new Date(project.data.startDate))}
                          </time>
                          {project.data.status && (
                            <div class="badge badge-success badge-sm">
                              {project.data.status}
                            </div>
                          )}
                        </div>
                      </div>
                    </div>

                    <h3 class="card-title text-2xl text-glow mb-4 hover:text-primary transition-colors">
                      <a href={`/projects/${project.slug}`}>
                        {project.data.title}
                      </a>
                    </h3>

                    <p class="text-base-content mb-6 leading-relaxed">
                      {project.data.description}
                    </p>

                    {/* Metrics if available */}
                    {project.data.metrics && Object.keys(project.data.metrics).length > 0 && (
                      <div class="grid grid-cols-2 gap-4 p-4 bg-base-200/50 rounded-lg mb-6">
                        {project.data.metrics.users && (
                          <div class="text-center">
                            <div class="text-sm opacity-70">Users</div>
                            <div class="text-2xl font-bold text-primary">{project.data.metrics.users.toLocaleString()}</div>
                          </div>
                        )}
                        {project.data.metrics.costSavings && (
                          <div class="text-center">
                            <div class="text-sm opacity-70">Cost Savings</div>
                            <div class="text-lg font-bold text-primary">{project.data.metrics.costSavings}</div>
                          </div>
                        )}
                        {project.data.metrics.performance && (
                          <div class="text-center col-span-2">
                            <div class="text-sm opacity-70">Performance</div>
                            <div class="text-lg font-bold text-primary">{project.data.metrics.performance}</div>
                          </div>
                        )}
                        {project.data.metrics.impact && (
                          <div class="text-center col-span-2">
                            <div class="text-sm opacity-70">Impact</div>
                            <div class="text-lg font-bold text-primary">{project.data.metrics.impact}</div>
                          </div>
                        )}
                      </div>
                    )}

                    {/* Technology stack */}
                    <div class="flex flex-wrap gap-1.5 mb-6">
                      {project.data.technologies?.slice(0, 4).map((tech: string) => (
                        <div class="badge badge-outline badge-sm hover:badge-primary transition-colors cursor-default flex-shrink-0">
                          {tech}
                        </div>
                      ))}
                      {project.data.technologies && project.data.technologies.length > 4 && (
                        <div class="badge badge-ghost badge-sm flex-shrink-0">
                          +{project.data.technologies.length - 4} more
                        </div>
                      )}
                    </div>

                    <div class="card-actions justify-between items-center">
                      <a 
                        href={`/projects/${project.slug}`} 
                        class="btn btn-primary btn-sm"
                      >
                        View Project
                      </a>
                      
                      <div class="flex gap-2">
                        {project.data.github && (
                          <a 
                            href={project.data.github} 
                            target="_blank"
                            rel="noopener noreferrer"
                            class="btn btn-ghost btn-sm btn-square"
                            title="View on GitHub"
                          >
                            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                              <path d="M12 0C5.374 0 0 5.373 0 12 0 17.302 3.438 21.8 8.207 23.387c.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/>
                            </svg>
                          </a>
                        )}
                        {project.data.demo && (
                          <a 
                            href={project.data.demo} 
                            target="_blank"
                            rel="noopener noreferrer"
                            class="btn btn-ghost btn-sm btn-square"
                            title="View Live"
                          >
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                            </svg>
                          </a>
                        )}
                      </div>
                    </div>
                    </div>
                  </article>
                );
              })}
            </div>
          </section>
        )}

        <!-- All Projects by Category -->
        <section>
          <h2 class="text-2xl font-bold text-glow mb-8 flex items-center gap-3">
            <span class="w-1 h-6 bg-accent rounded-full"></span>
            All Projects
          </h2>
          
          {Object.entries(projectsByCategory).map(([category, projects]) => {
            const meta = categoryMeta[category as keyof typeof categoryMeta];
            return (
              <div class="mb-12">
                <div class="flex items-center gap-4 mb-8">
                <span class="text-2xl">{meta?.icon || '📁'}</span>
                <div>
                <h3 class="text-xl font-bold text-primary">
                {meta?.title || category.replace('-', ' ')}
                </h3>
                <p class="text-base-content opacity-70 text-sm">
                {meta?.description || `${projects.length} projects in this category`}
                </p>
                </div>
                <div class="ml-auto">
                <div class="badge badge-primary">
                {projects.length} {projects.length === 1 ? 'project' : 'projects'}
                </div>
                </div>
                </div>
                
                <div class="grid sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-4 gap-4 lg:gap-6">
                  {projects.map((project) => (
                    <article class="card bg-base-100 shadow-xl hover:shadow-2xl transition-all duration-300 group hover:scale-[1.01]">
                      <div class="card-body p-4 lg:p-6">
                      {/* Subtle gradient overlay */}
                      <div class="absolute inset-0 bg-gradient-to-br from-transparent via-transparent to-accent/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                      <div class="relative z-10">
                      <div class="flex items-start justify-between mb-4">
                        <div class="flex items-center gap-2">
                          <time class="text-sm text-base-content opacity-70">
                            {formatDate(new Date(project.data.startDate))}
                          </time>
                          {project.data.featured && (
                            <div class="badge badge-primary badge-sm">
                              Featured
                            </div>
                          )}
                        </div>
                        <div class="w-2 h-2 rounded-full bg-accent opacity-0 group-hover:opacity-100 transition-opacity"></div>
                      </div>
                      
                      <h4 class="card-title text-xl text-glow mb-3 hover:text-primary transition-colors leading-tight">
                        <a href={`/projects/${project.slug}`}>
                          {project.data.title}
                        </a>
                      </h4>
                      
                      <p class="text-base-content mb-4 leading-relaxed line-clamp-3">
                        {project.data.description}
                      </p>
                      
                      {/* Technology tags */}
                      <div class="flex flex-wrap gap-1.5 mb-4">
                        {project.data.technologies?.slice(0, 3).map((tech: string) => (
                          <div class="badge badge-outline badge-sm flex-shrink-0">
                            {tech}
                          </div>
                        ))}
                        {project.data.technologies && project.data.technologies.length > 3 && (
                          <div class="badge badge-ghost badge-sm flex-shrink-0">
                            +{project.data.technologies.length - 3}
                          </div>
                        )}
                      </div>
                      </div>
                      
                      <div class="card-actions justify-between items-center">
                        <a 
                          href={`/projects/${project.slug}`} 
                          class="btn btn-primary btn-sm btn-outline"
                        >
                          View details
                          <svg class="ml-1 w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                          </svg>
                        </a>
                        
                        <div class="flex gap-2">
                          {project.data.github && (
                            <a 
                              href={project.data.github} 
                              target="_blank"
                              rel="noopener noreferrer"
                              class="btn btn-ghost btn-xs btn-square"
                              title="GitHub"
                            >
                              <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                                <path d="M12 0C5.374 0 0 5.373 0 12 0 17.302 3.438 21.8 8.207 23.387c.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23A11.509 11.509 0 0112 5.803c1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.30.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576C20.566 21.797 24 17.3 24 12c0-6.627-5.373-12-12-12z"/>
                              </svg>
                            </a>
                          )}
                          {project.data.demo && (
                            <a 
                              href={project.data.demo} 
                              target="_blank"
                              rel="noopener noreferrer"
                              class="btn btn-ghost btn-xs btn-square"
                              title="Live Demo"
                            >
                              <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
                              </svg>
                            </a>
                          )}
                        </div>
                      </div>
                      </div>
                    </article>
                  ))}
                </div>
              </div>
            );
          })}
        </section>
      </>
    ) : (
      <!-- Empty state -->
      <section class="text-center py-16">
        <div class="hero bg-base-100 shadow-xl rounded-2xl">
          <div class="hero-content text-center">
            <div class="max-w-2xl">
              <div class="w-20 h-20 mx-auto mb-6 rounded-full bg-primary/20 border border-primary/30 flex items-center justify-center">
                <svg class="w-10 h-10 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
                </svg>
              </div>
              <h2 class="text-3xl font-bold text-glow mb-4">Portfolio Coming Soon</h2>
              <p class="text-base-content opacity-70 mb-8 leading-relaxed">
                I'm currently documenting and showcasing my platform engineering projects, 
                open source contributions, and real-world case studies. This will include 
                detailed breakdowns of architecture decisions, implementation approaches, 
                and measurable outcomes.
              </p>
              <div class="grid md:grid-cols-3 gap-4 mb-8">
                {Object.entries(categoryMeta).map(([key, meta]) => (
                  <div class="card bg-base-200 shadow">
                    <div class="card-body p-4 text-center">
                      <div class="text-2xl mb-2">{meta.icon}</div>
                      <h3 class="card-title text-primary mb-1 text-sm justify-center">{meta.title}</h3>
                      <p class="text-xs text-base-content opacity-70">{meta.description}</p>
                    </div>
                  </div>
                ))}
              </div>
              <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="/about" class="btn btn-primary">
                  Learn About My Experience
                </a>
                <a href="/guides" class="btn btn-secondary">
                  Browse Technical Guides
                </a>
              </div>
            </div>
          </div>
        </div>
      </section>
    )}

    <!-- Call to Action -->
    <section class="mt-16">
      <div class="card bg-base-100 shadow-xl text-center">
        <div class="card-body">
          <h2 class="card-title text-2xl text-glow mb-4 justify-center">Interested in Collaboration?</h2>
          <p class="text-base-content opacity-70 mb-6 max-w-4xl mx-auto">
            I'm always open to discussing platform engineering challenges, 
            sharing experiences, or exploring collaboration opportunities on 
            interesting technical problems.
          </p>
          <div class="card-actions justify-center">
            <a href="/contact" class="btn btn-primary">
              Get in Touch
            </a>
            <a 
              href="https://github.com/GeoffMillerAZ" 
              target="_blank"
              rel="noopener noreferrer"
              class="btn btn-secondary"
            >
              View GitHub Profile
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .text-glow {
    text-shadow: 0 0 10px #f92aad;
  }
</style>